---
title: レジストリ
---

## 完全カタログ

レジストリページは `registry/sources/free-for-dev/normalized.json` を直接読み込み、`free-for-dev` から取り込んだ候補カタログ全体を参照します。

## データ状態

各エントリには、ソースカテゴリ、無料枠の状態、レビュー状態、元ソース行の参照、ローカライズ用フィールドが保持されます。`needs_review` は人手確認がまだ必要であることを示します。
